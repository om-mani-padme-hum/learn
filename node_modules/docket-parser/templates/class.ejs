      <hr>
      <div id='<%-id%>' class='row'>
        <div class='col'>
          <h3 class='mt-2 pl-1'>Class: <%-title%> <small>[<a class='<%-linkClass%>' href='#top'>top</a>]</small></h3>
        </div>
      </div>
      <div class='pl-3'>
        <% if ( status ) { %>
        <div class='row'>
          <div class='col-auto'>
            <div class='alert mt-2 alert-<%-status.color()%>'>
              <strong><%-status.status(true)%></strong>: <%-status.description()%>
            </div>
          </div>
        </div>
        <% } %>
        <% if ( copyright.length > 0 || authors.length > 0 ) { %>
        <div class='row'>
          <div class='col'>
            <p>
        <% } %>
        <% if ( copyright.length > 0 ) { %>
              <small>Copyright &copy; <%-copyright%></small>
              <br>
        <% } %>
        <% if ( authors.length > 0 ) { %>
              <small>Written by: <%-authors%></small>
        <% } %>
        <% if ( copyright.length > 0 || authors.length > 0 ) { %>
            </p>
          </div>
        </div>
        <% } %>
        <% if ( added || updates.length > 0 ) { %>
        <div class='row'>
          <div class='col-auto'>
            <p>
              <small>&#9656; <a class='<%-linkClass%>' data-toggle='collapse' href='#collapse<%-count%>'>History</a></small>
            </p>
            <div class='collapse' id='collapse<%-count%>'>
              <div class='card card-body p-1 px-3 ml-3 mb-2'>
        <% } %>
        <% if ( added ) { %>
                <small>Added in: <%-added.version()%> <%-added.description()%></small>
        <% } %>
        <% if ( updates.length > 0 ) { %>
        <%   updates.forEach((update) => { %>
                <small>Updated in: <%-update.version()%> <%-update.description()%></small>
        <%   }); %>
        <% } %>
        <% if ( added || updates.length > 0 ) { %>
              </div>
            </div>  
          </div>
        </div>
        <% } %>
        <div class='row'>
          <div class='col'>
            <ul>
              <li>&lt;<a class='<%-linkClass%>' href='https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object'>Object</a>&gt;</li>
            </ul>
          </div>
        </div>
        <% if ( description.length > 0 ) { %>
        <div class='row'>
          <div class='col'>
            <p><%-description%></p>
          </div>
        </div>
        <% } %>
      </div>
      